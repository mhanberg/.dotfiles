#!/usr/bin/env bash

case "$1" in
  .dotfiles)
    echo "$HOME/.dotfiles"
    ;;

  notes)
    echo "$ICLOUD/notes/personal"
    ;;

  work-notes)
    echo "$ICLOUD/notes/work"
    ;;

  *)
    echo "$HOME/src/$1"
    ;;
esac
