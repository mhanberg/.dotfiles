#!/usr/bin/env bash

case "$1" in
  .dotfiles)
    echo "$HOME/.dotfiles"
    ;;

  notes)
    echo "$ICLOUD/notes/zk"
    ;;

  work-notes)
    echo "$ICLOUD/stord/notes"
    ;;
  *)

    echo "$HOME/Development/$1"
    ;;
esac
